<?xml version="1.0" encoding="UTF-8"?>

<select id="getImat" resultType="com.itwillbs.domain.ImatDTO">
        select * 
        from members 
        where id = #{id} and pass = #{pass}
    </select>
    